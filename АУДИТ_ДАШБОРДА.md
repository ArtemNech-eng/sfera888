# 🔍 ПОЛНЫЙ АУДИТ ДАШБОРДА

**Дата:** 2026-05-12  
**Статус:** В процессе  
**Проверено:** API, логика расчётов, UI/UX

---

## ✅ ЧТО РАБОТАЕТ ПРАВИЛЬНО

### 1. API Эндпоинты (`/api/analytics/dashboard-v2`)

**Структура данных:**
- ✅ Все данные загружаются параллельно (Promise.all) — оптимально
- ✅ Кэширование баланса Авито (TTL 5 мин) — не блокирует основной запрос
- ✅ Обработка ошибок при запросе к Avito API (fallback на ручной баланс)
- ✅ Таймаут 4 секунды для внешних запросов

**Расчёт выручки:**
```typescript
// Корректная логика: комиссия + предоплаты (только для заказов без оплаченной комиссии)
const commission = txRows.filter(t => t.paymentStatus === "paid" && t.paidAt >= start && t.paidAt < end)
  .reduce((s, t) => s + Number(t.commission), 0);
const prepayments = receiptRows.filter(r => 
  r.prepaymentSubmittedAt >= start && 
  r.prepaymentSubmittedAt < end &&
  !paidTxOrderIds.has(r.orderId) // ✅ Исключает двойной подсчёт
).reduce((s, r) => s + Number(r.prepaymentAmount ?? 0), 0);
```
✅ **Нет двойного подсчёта** — предоплаты учитываются только если комиссия ещё не оплачена

### 2. KPI Карточки

**Доход сегодня:**
- ✅ Сравнение с вчера (vs вчера)
- ✅ Прогресс-бар относительно среднего дневного дохода месяца
- ✅ Анимация при изменении значений (flash-green/flash-red)

**Доход за месяц:**
- ✅ Сравнение с прошлым месяцем
- ✅ Использует реальное количество дней месяца (не хардкод 30)

**Заявки сегодня:**
- ✅ Сравнение с вчера
- ✅ Подсчёт из таблицы leads по createdAt

**Оплаты сегодня:**
- ✅ Подсчёт оплаченных смет (prepaymentSubmittedAt)
- ✅ Сравнение с вчера

**Активные мастера:**
- ✅ Прогресс-бар: активные / всего
- ✅ Фильтр по статусу "active"

**Новые мастера:**
- ✅ Сравнение с вчера

**Баланс Авито:**
- ✅ Кастомная карточка с кнопкой "Обновить"
- ✅ Красная подсветка при балансе < 1000₽
- ✅ Анимация мигания точки при низком балансе
- ✅ Модальное окно для ручного обновления баланса

### 3. Алерты (AlertsBlock)

**Логика определения:**
- ✅ Заказы без мастера > 2ч (critical)
- ✅ Нет сметы > 24ч (critical)
- ✅ Нет оплаты > 48ч (warning)
- ✅ Просроченные комиссии (warning)

**UI:**
- ✅ Красный фон для критичных, жёлтый для предупреждений
- ✅ Кликабельные бейджи с переходом на фильтрованные списки
- ✅ Кнопка "Все проблемы" ведёт на первый критичный алерт

### 4. Прогноз месяца (ForecastCard)

**Расчёт:**
```typescript
const dailyAvg = daysPassed > 0 ? revenueMonth / daysPassed : 0;
const forecast = Math.round(dailyAvg * daysInMonth);
const goal = 3_000_000; // ✅ Цель захардкожена
const progressPct = goal > 0 ? Math.round((forecast / goal) * 100) : 0;
```

**Статусы:**
- ✅ `ahead` (опережение) — прогресс >= 110%
- ✅ `on_track` (по плану) — прогресс >= 90%
- ✅ `behind` (отстаём) — прогресс < 90%

**UI:**
- ✅ Прогресс-бар дней месяца
- ✅ 3 метрики: заработано / прогноз / цель
- ✅ Цветовая индикация статуса
- ✅ Прогресс-бар к цели

### 5. Риск-монитор (RiskMonitor)

**Логика:**
- ✅ Показывает заказы старше 2 часов
- ✅ Уровни риска:
  - Без мастера >4ч → critical
  - Без мастера 2-4ч → warning
  - Нет сметы >24ч → critical
  - Нет сметы 2-24ч → warning
  - Нет оплаты >72ч → critical
  - Нет оплаты 2-72ч → warning

**Расчёт ожидаемой комиссии:**
```typescript
expected_commission: tx ? Number(tx.commission) : Math.round((Number(orderAmount ?? 0) || 50000) * 0.15)
```
✅ Использует реальную комиссию из транзакции, если есть  
✅ Fallback: 15% от суммы заказа или 50000₽ * 0.15 = 7500₽

**UI:**
- ✅ 3 карточки: критичных / внимания / под угрозой (₽)
- ✅ Список до 5 заказов с цветовой индикацией
- ✅ Клик по заказу → переход на страницу заказа
- ✅ Кнопка "Ещё N заказов" если больше 5

### 6. График доходов (RevenueChart)

**Данные:**
- ✅ Строится за 90 дней, показывает последние 30/60/90
- ✅ Переключатель периодов (30д / 60д / 90д)
- ✅ Recharts с градиентной заливкой
- ✅ Средняя линия (ReferenceLine)
- ✅ Кастомный тултип с форматированием

**Метрики под графиком:**
- ✅ Всего за период
- ✅ Средний день
- ✅ Лучший день (дата + сумма)

### 7. Воронка конверсии (FunnelCard)

**Этапы:**
1. Контакты (все лиды)
2. Заявки (исключая non_target, client_refusal)
3. Назначен мастер
4. Отправлена смета
5. Получена оплата
6. Завершено

**Расчёт конверсии:**
- ✅ Общая: завершённые / контакты * 100%
- ✅ Сравнение с прошлым месяцем

**UI:**
- ✅ Визуальная воронка с процентами на каждом этапе
- ✅ Цветовая индикация изменения конверсии

### 8. Лента активности (LiveFeed)

**События (последние 24ч):**
- ✅ Оплаты предоплат (до 8)
- ✅ Новые заявки (до 5)
- ✅ Завершённые заказы (до 5)
- ✅ Новые мастера (до 3)

**Сортировка:**
- ✅ По timestamp (desc) — новые сверху
- ✅ Лимит 20 событий

**UI:**
- ✅ Иконки по типу события
- ✅ Время в формате "X назад"
- ✅ Город и сумма (если есть)

### 9. Метрики скорости (SpeedMetrics)

**Расчёт:**
- ✅ Сравнение текущего периода (30 дней) с предыдущим (30-60 дней назад)
- ✅ Метрики:
  - Назначение мастера (мин)
  - Отправка сметы (ч)
  - Получение оплаты (ч)
  - Завершение работ (дн)
  - Полный цикл (дн)

**Нормы:**
- ✅ Назначение: 30 мин
- ✅ Смета: 6 ч
- ✅ Оплата: 12 ч
- ✅ Завершение: 3 дн
- ✅ Цикл: 7 дн

**UI:**
- ✅ Цветовая индикация: зелёный (лучше нормы), жёлтый (норма), красный (хуже)
- ✅ Стрелки вверх/вниз для сравнения с прошлым периодом

### 10. Города (CitiesCard)

**Данные по городам:**
- ✅ Заявки
- ✅ Оплаты
- ✅ Выручка
- ✅ Мастера (всего / активные)
- ✅ Конверсия (%)

**Сортировка:**
- ✅ По выручке (desc)
- ✅ Топ-6 городов

### 11. ROI по источникам (ROICard)

**Источники:**
- Авито, Сайт, Директ, Звонки, Сарафан, Повторный, Другое

**Расчёт выручки:**
- ✅ Комиссии + предоплаты по лидам из источника
- ✅ Конверсия: завершённые / лиды * 100%

**Сортировка:**
- ✅ По выручке (desc)

### 12. Топ мастеров (TopMasters)

**Метрики:**
- ✅ Выручка (комиссии)
- ✅ Заказы
- ✅ Рейтинг

**Сортировка:**
- ✅ По выручке (desc)
- ✅ Топ-10

### 13. Последние заказы (RecentOrders)

**Данные:**
- ✅ Последние 10 заказов (по createdAt)
- ✅ Статус, мастер, город, сумма

---

## ⚠️ НАЙДЕННЫЕ ПРОБЛЕМЫ

### 🔴 КРИТИЧНЫЕ

#### 1. **Цель месяца захардкожена**
**Файл:** `artifacts/api-server/src/routes/analytics.ts:187`
```typescript
const goal = 3_000_000; // ❌ Хардкод
```
**Проблема:** Цель не настраивается, всегда 3 млн ₽  
**Решение:** Вынести в настройки (system_settings) или сделать редактируемой через UI

#### 2. **Расчёт метрик скорости использует приблизительные коэффициенты**
**Файл:** `artifacts/api-server/src/routes/analytics.ts:331`
```typescript
return { 
  assignH: avgLifecycleH * 0.15,  // ❌ Магическое число
  lifecycleD: avgLifecycleH / 24 
};
```
**Проблема:** Время назначения мастера рассчитывается как 15% от полного цикла — неточно  
**Решение:** Использовать реальное поле `assignedAt` из orders

#### 3. **Двойной подсчёт предоплат в ROI**
**Файл:** `artifacts/api-server/src/routes/analytics.ts:398`
```typescript
const revenue = srcOrders.reduce((s, o) => {
  const tx = txRows.filter(t => t.orderId === o.id && t.paymentStatus === "paid")
    .reduce((ss, t) => ss + Number(t.commission), 0);
  const pr = receiptRows.filter(r => r.orderId === o.id && r.prepaymentSubmittedAt)
    .reduce((ss, r) => ss + Number(r.prepaymentAmount), 0);
  return s + tx + pr; // ❌ Может быть двойной подсчёт
}, 0);
```
**Проблема:** Если комиссия оплачена И есть предоплата — считается дважды  
**Решение:** Использовать ту же логику что в calcIncome (проверка paidTxOrderIds)

### 🟡 СРЕДНИЕ

#### 4. **Нет обработки пустых данных в графике**
**Файл:** `artifacts/crm/src/components/dashboard/RevenueChart.tsx:59`
```typescript
const best = chartData.reduce((best, d) => d.amount > best.amount ? d : best, chartData[0] || { date: '', amount: 0 });
```
**Проблема:** Если chartData пустой, best.date будет пустой строкой  
**Решение:** Добавить проверку `chartData.length > 0` перед reduce

#### 5. **Фильтр по городам не работает для некоторых компонентов**
**Файл:** `artifacts/crm/src/pages/dashboard.tsx:199`
```typescript
const cities = city === "Все города" ? citiesRaw : citiesRaw.filter((c: any) => c.city === city);
```
**Проблема:** Фильтр применяется только к CitiesCard, но не к KPI, графику, воронке  
**Решение:** Либо убрать фильтр городов, либо применять ко всем данным

#### 6. **Нет индикации загрузки при обновлении баланса Авито**
**Файл:** `artifacts/crm/src/pages/dashboard.tsx:163`
```typescript
const handleAvitoBalanceSave = useCallback(async (val: number) => {
  // ... нет индикатора загрузки в UI
}, [refetch]);
```
**Проблема:** Пользователь не видит что запрос выполняется  
**Решение:** Добавить спиннер или disabled состояние кнопки

### 🟢 МЕЛКИЕ

#### 7. **Нет обработки ошибок при загрузке дашборда**
**Файл:** `artifacts/crm/src/pages/dashboard.tsx:31`
```typescript
async function fetchDashboard() {
  const resp = await fetch("/api/analytics/dashboard-v2", { credentials: "include" });
  if (!resp.ok) throw new Error("Failed to fetch dashboard"); // ❌ Ошибка не показывается пользователю
  return resp.json();
}
```
**Проблема:** При ошибке API пользователь видит бесконечную загрузку  
**Решение:** Добавить error state и показать сообщение об ошибке

#### 8. **Нет дебаунса при изменении фильтров**
**Проблема:** При быстром переключении периодов/городов может быть много запросов  
**Решение:** Добавить debounce или использовать staleTime в react-query (уже есть 30 сек)

#### 9. **Кнопка "Все проблемы" в AlertsBlock может вести на неправильную страницу**
**Файл:** `artifacts/crm/src/components/dashboard/AlertsBlock.tsx:58`
```typescript
const critical = alerts.find(a => a.type === 'critical');
const target = critical?.link ?? alerts[0]?.link ?? '/orders';
```
**Проблема:** Если критичный алерт есть, но его link не соответствует первому алерту в списке, может быть путаница  
**Решение:** Либо всегда вести на `/orders` с фильтром, либо уточнить логику

#### 10. **Нет кнопки "Экспорт в Excel/CSV"**
**Проблема:** Нельзя выгрузить данные дашборда  
**Решение:** Добавить кнопку экспорта (опционально)

---

## 🎯 РЕКОМЕНДАЦИИ ПО УЛУЧШЕНИЮ

### Высокий приоритет

1. **Исправить двойной подсчёт в ROI** (критично для финансов)
2. **Сделать цель месяца настраиваемой** (бизнес-требование)
3. **Улучшить расчёт метрик скорости** (использовать реальные даты)
4. **Добавить обработку ошибок** (UX)

### Средний приоритет

5. **Применить фильтр городов ко всем компонентам** или убрать его
6. **Добавить индикацию загрузки** при обновлении баланса Авито
7. **Проверить пустые данные** в графике

### Низкий приоритет

8. **Добавить экспорт данных** (опционально)
9. **Добавить настройку целей** по городам/источникам
10. **Добавить сравнение с прошлым годом** (если есть данные)

---

## 📊 ИТОГОВАЯ ОЦЕНКА

**Общая оценка:** 8.5/10

**Что работает отлично:**
- ✅ Производительность (параллельная загрузка, кэширование)
- ✅ UI/UX (анимации, цветовая индикация, адаптивность, hover-эффекты)
- ✅ Логика расчётов (в основном корректная)
- ✅ Нет двойного подсчёта в основной выручке
- ✅ Все кнопки и навигация работают корректно
- ✅ Accessibility (role, tabIndex, onKeyDown для интерактивных элементов)
- ✅ Прогресс-бары и индикаторы статусов
- ✅ Кастомные тултипы и модальные окна

**Что нужно исправить:**
- ⚠️ Двойной подсчёт в ROI (критично)
- ⚠️ Захардкоженная цель (бизнес-требование)
- ⚠️ Приблизительные метрики скорости (точность данных)
- ⚠️ Обработка ошибок (UX)
- ⚠️ Фильтр городов не применяется ко всем компонентам

**Вывод:** Дашборд работает хорошо, UI/UX на высоком уровне, все кнопки и навигация функционируют корректно. Основные проблемы — в логике расчётов (ROI, метрики скорости) и отсутствии обработки ошибок.
