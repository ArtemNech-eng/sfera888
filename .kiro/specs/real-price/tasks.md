# Tasks — Real Price (Реальная цена)

> Формат: фазы с чекбоксами, ссылки на Requirements. Сроки не указываем.
> Прототипы согласованы: `object-card-master-v1.html` (форма мастера),
> `object-page-public-v1.html` (страница на сайте).

## Фаза 0 — Фундамент данных
- [x] 0.1 Словарь `work_types` (slug, name, category `project|task`, default_unit, synonyms). (Req 2.1)
- [x] 0.2 Расширить `receipts` additive-полями: `object_type, source, area, zhk, stages,
      is_published, published_at, is_indexable, publish_consent, slug, seo_*`. (Req 1.1)
- [x] 0.3 Идемпотентная миграция `ADD COLUMN IF NOT EXISTS` + файл-зеркало. Не ломает 25+ зависимостей. (Req 1.1, 1.2)
- [x] 0.4 Консолидация: 1 заказ = 1 Объект (слить существующие множественные receipts заказа). (Req 1.3)
      _Инвариант соблюдён на чтении: `listObjectsForMaster` дедуплицирует по канонической (последней) расписке заказа (`objectDedupe.ts`, 5 юнит-тестов). Физический merge строк receipts (25+ зависимостей) не автоматизируем — admin dry-run отчёт `POST /api/real-price/consolidate` показывает кандидатов для ручного слияния на стейджинге._
- [x] 0.5 Таблица `price_points` (нормализованные ценовые точки) + индексы. (Req 3.3)
- [x] 0.6 Полу-ручной маппинг 36 существующих смет → `work_types` (backfill авто-матчингом, 76/79). (Req 2.4)

## Фаза 1 — Агрегация
- [x] 1.1 Хук «заказ завершён → сформировать price_points из stages (только с work_type_id)». (Req 3.1, 3.2)
- [x] 1.2 Таблица `price_aggregates` + пересчёт (percentile_cont, отсечение выбросов). Расширить SQL из `/raboty/market-stats`. (Req 4.1, 4.2)
- [x] 1.3 Пороги `N` (город/ЖК) через `seoContentThreshold`; `is_indexable`; склейка ЖК→город→страна. (Req 4.4)
- [x] 1.4 Пересчёт по завершению заказа + ночной cron для series_12m. (Req 4.3)
      _Пересчёт при публикации был; добавлен ночной пересчёт `recomputePriceAggregates()` в 03:30 МСК (дневной тик `index.ts`), перестраивающий `series_12m`._

## Фаза 2 — Ценовые страницы + SEO
- [x] 2.1 `/ceny/{услуга}/{город}/` и `/ceny/{услуга}/{город}/{жк}/` (SSG/ISR из `price_aggregates`). (Req 5.1)
- [x] 2.2 Контент страницы: медиана, P25–P75, график 12 мес, `n` + дата, 5 анонимных примеров. (Req 5.2)
- [x] 2.3 Schema.org `Dataset` + `FAQPage`. (Req 5.3)
- [x] 2.4 `noindex` при `is_indexable=false`; sitemap/IndexNow/revalidate (переиспользуем). (Req 4.4, 5.5)
- [x] 2.5 `/about/method` — методика расчёта. (Req 5.6)

## Фаза 3 — Проверятор смет (виральный вход)
- [x] 3.1 `/proverit-smetu`: ввод/вставка позиций → построчный вердикт vs медиана (🟢/🟡/🔴). (Req 7.1)
- [x] 3.2 OG-картинка «поделиться результатом». (Req 7.2)
- [x] 3.3 Конверсия в лид. (Req 7.3)
- [x] 3.4 (Под-фаза) LLM-разбор PDF/фото сметы → структура. (Req 7.4)
      _Загрузка в `CheckForm` → прокси `/api/real-price/parse-estimate` → `api-server` (multer + OpenAI vision для фото, Responses API для PDF) → предзаполнение позиций. Чистый нормализатор `estimateNormalize.ts` + 8 юнит-тестов; изолировано (без ключа OpenAI → 503)._

## Фаза 4 — Индекс
- [x] 4.1 `/indeks/` v0 (Краснодар): уровень цен по месяцам. (Req 8.1, 8.3)
- [x] 4.2 Помесячные срезы для квартальных пресс-релизов. (Req 8.2)

## Фаза 5 — Карточка Объекта в /cabinet + кейс-страница + Owner_Mode
- [x] 5.1 Форма Объекта в `/cabinet`: клиент/адрес (приватно), тип, площадь, **этапы+позиции**
      (выбор из `work_types`), фото до/после, публикация+согласие. (Req 1.4, 2.2, 9.3) — по прототипу мастера.
- [x] 5.2 Авто-публикация проекта при завершении, если контент богат + `publish_consent`. (Req 6.2, 6.3)
- [x] 5.3 Кейс-страница на базе `/raboty/{slug}`: значок «Подтверждено», смета по этапам,
      блок «Реальная цена» (сумма vs медиана), мастер (с согласия), CTA «Хочу такой же». (Req 6.1, 6.4) — по прототипу страницы.
- [x] 5.4 Owner_Mode inline-контролы на `/master/{slug}` и `/raboty/{slug}`. (Req 10.2)
      _Клиентская детекция владельца: профиль/легаси-кейс — по id (`/api/cabinet/auth/me`), объектная кейс-страница — по membership slug (`/api/cabinet/objects`, без раскрытия masterId). Плавающий `OwnerBar` с deep-link в редактор. Чистая логика + 6 юнит-тестов в `lib/ownerMode.ts`._
- [x] 5.5 Единая Zen-шапка для залогиненного мастера + аватар-меню + «＋ Создать объект». (Req 10.1)
      _Клиентская детекция сессии в `components/Header.tsx` через `GET /api/cabinet/auth/me` (без `cookies()` в root layout → SSG/ISR сохранены). Чистая логика + 5 юнит-тестов в `lib/headerSession.ts` / `headerSession.test.ts`._
- [x] 5.6 (доп.) Хаб «Мои Объекты» `/cabinet/objects` — список черновиков/опубликованных.

## Дорожка M — Миграция master-PWA → единый режим (вариант B)
> Слияние кабинетов: доставлен **opt-in редирект (вариант A)** за флагом
> `UNIFY_CABINET_REDIRECT` (по умолчанию ВЫКЛ) — старый `/master-pwa/*`
> перенаправляется в единый `/cabinet/*`, push-диплинки сохраняются
> (`cabinetRedirect.ts` + 5 тестов). Полный вариант B (тот же домен без
> повторного входа) — реверс-прокси Next через api-server, требует
> инфраструктуры/стейджинга.
- [ ] M.1 Отдавать единый кабинет под привычным мастеру доменом/входом (сохранить домен/сессию/push). (Req 11.2)
      _Частично: opt-in редирект `/master-pwa → /cabinet` (вариант A). Вариант B (тот же домен) — инфра/реверс-прокси, ожидает Railway/стейджинг._
- [ ] M.2 Проверить непрерывность входа (общая сессия `masterId`) и данных (общий бэкенд). (Req 11.1)
      _Бэкенд/БД/сессия уже общие; при варианте A нужен один вход на домене marketplace._
- [ ] M.3 Push: переподписка/скоуп, чтобы уведомления не замолкли. (Req 11.3)
- [x] M.4 Диплинки из пушей/Max-бота → актуальные экраны. (Req 11.4)
      _`mapMasterPwaPathToCabinet` сопоставляет legacy-маршруты (orders/chat/balance/wallet/profile…) экранам кабинета; применяется в редиректе._
- [ ] M.5 Сфокусированный рабочий стол (заказы/статусы/смета/баланс/чат) в Zen-мире, app-вид на мобиле. (Req 10.3)
      _По сути уже реализован как `/cabinet/*` (nav/topbar + экраны orders/chat/balance/wallet/objects/…)._

## Приватность (сквозное, проверять в каждой фазе)
- [ ] P.1 Точный адрес/данные клиента не публикуются (только ЖК/район). (Req 9.1)
- [ ] P.2 Имя мастера — только на кейс-странице с согласия; не в агрегат. (Req 9.2)
- [ ] P.3 Фото — только при `publish_consent`. (Req 9.3)
- [ ] P.4 Публично — только агрегаты и анонимные примеры. (Req 9.4)

## Проверка перед мержем (каждая фаза)
- [ ] `pnpm --filter @workspace/marketplace run typecheck` и `run build`
- [ ] `pnpm --filter @workspace/api-server run typecheck` (+ тесты, где затронут paymentState/receipts)
- [ ] Прогон миграций на стейджинге; проверка, что текущий receipt-флоу (оплата/подтверждение) не сломан

## Метрики (еженедельно)
- [ ] Число закрытых сделок в базе (проекты/задачи)
- [ ] Число опубликованных страниц (агрегаты/кейсы), покрытие услуга×город прошедших порог n
- [ ] Поисковый трафик, позиции по целевым запросам
- [ ] Доля посетителей проверятора, поделившихся вердиктом
- [ ] Цитирования в СМИ / обратные ссылки

## Порядок реализации (рекомендация)
Фаза 0 → 1 → (5.1–5.3 карточка+кейс, чтобы был контент) → 2 (ценовые страницы) →
3 (проверятор — трафик) → 4 (индекс). Дорожка M и Owner_Mode (5.4–5.5) — параллельно,
отдельными PR. Каждая фаза — отдельный PR с предпросмотром (полный `next build` — на вашей
стороне/Railway: в песочнице Google Fonts недоступны).
