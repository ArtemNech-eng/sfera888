[build]
buildCommand = "pnpm run build"

[build.environment]
BASE_PATH = "/crm/"

[deploy]
preDeployCommand = "pnpm run db:migrate && pnpm --filter @workspace/db run push"

[deploy.environment]
DATABASE_URL = "${{Postgres-BkOp.DATABASE_URL}}"
BASE_PATH = "/crm/"
