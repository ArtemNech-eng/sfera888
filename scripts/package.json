{
  "name": "@workspace/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "hello": "tsx ./src/hello.ts",
    "seed": "tsx ./src/seed.ts",
    "seed-ai-designs": "tsx ./src/seed-ai-designs.ts",
    "test-render": "tsx ./src/test-render.ts",
    "db-migrate": "tsx ./src/db-migrate.ts",
    "identity-preservation-pilot": "tsx ./src/identity-preservation-pilot.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@workspace/db": "workspace:*",
    "bcryptjs": "^3.0.2"
  },
  "//optionalDependencies": "@xenova/transformers used by identity-preservation-pilot.ts (CLIP image similarity). Install with: pnpm --filter @workspace/scripts add @xenova/transformers",
  "optionalDependencies": {
    "@xenova/transformers": "^2.17.2"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@types/bcryptjs": "^2.4.6",
    "tsx": "catalog:"
  }
}
